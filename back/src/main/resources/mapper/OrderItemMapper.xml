<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.village.revive.mapper.OrderItemMapper">

    <!-- 根据订单ID查询订单明细列表 -->
    <select id="selectByOrderId" resultType="com.village.revive.entity.OrderItem">
        SELECT * FROM order_items 
        WHERE order_id = #{orderId}
        ORDER BY created_at ASC
    </select>

    <!-- 根据订单ID删除订单明细 -->
    <delete id="deleteByOrderId">
        DELETE FROM order_items WHERE order_id = #{orderId}
    </delete>

    <!-- 批量插入订单明细 -->
    <insert id="batchInsert">
        INSERT INTO order_items (order_id, product_id, product_name, product_image, product_price, quantity, total_price, product_origin, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage}, #{item.productPrice}, #{item.quantity}, #{item.totalPrice}, #{item.productOrigin}, NOW())
        </foreach>
    </insert>

</mapper>
