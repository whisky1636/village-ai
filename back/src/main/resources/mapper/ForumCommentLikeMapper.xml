<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.village.revive.mapper.ForumCommentLikeMapper">

    <!-- 检查用户是否已点赞评论 -->
    <select id="existsByCommentIdAndUserId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM forum_comment_likes
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </select>

    <!-- 删除用户对评论的点赞 -->
    <delete id="deleteByCommentIdAndUserId">
        DELETE FROM forum_comment_likes
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </delete>

    <!-- 统计评论点赞数 -->
    <select id="countByCommentId" resultType="long">
        SELECT COUNT(*)
        FROM forum_comment_likes
        WHERE comment_id = #{commentId}
    </select>

</mapper>
