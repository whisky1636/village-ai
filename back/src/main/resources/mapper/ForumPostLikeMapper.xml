<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.village.revive.mapper.ForumPostLikeMapper">

    <!-- 检查用户是否已点赞帖子 -->
    <select id="existsByPostIdAndUserId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM forum_post_likes
        WHERE post_id = #{postId} AND user_id = #{userId}
    </select>

    <!-- 删除用户对帖子的点赞 -->
    <delete id="deleteByPostIdAndUserId">
        DELETE FROM forum_post_likes
        WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <!-- 统计帖子点赞数 -->
    <select id="countByPostId" resultType="long">
        SELECT COUNT(*)
        FROM forum_post_likes
        WHERE post_id = #{postId}
    </select>

</mapper>
